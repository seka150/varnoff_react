import { Box, styled } from '@mui/system';

const FlexBetweenComponent = styled(Box)({
  display: 'flex',
  justifyContent: 'flex-start',
  alignItems: 'center',
});

export default FlexBetweenComponent;
